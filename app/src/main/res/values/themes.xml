<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Neptune" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="splashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_back</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.Neptune</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>
</resources>

